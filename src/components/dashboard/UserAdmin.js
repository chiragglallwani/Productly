import React from "react";
import { connect } from "react-redux";

export const UserAdmin = (props) => {
  return <div>User Admin (This page is under development)</div>;
};

export default connect(null, null)(UserAdmin);
